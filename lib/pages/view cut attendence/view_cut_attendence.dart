import 'package:flutter/material.dart';

class ViewCutAttendence extends StatelessWidget {
  const ViewCutAttendence({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('View Cut Attendence'),
        centerTitle: true,
      ),
    );
  }
}
