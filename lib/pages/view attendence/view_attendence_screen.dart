import 'package:flutter/material.dart';

class ViewAttendenceScreen extends StatelessWidget {
  const ViewAttendenceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('View Attendence'),
        centerTitle: true,
      ),
    );
  }
}
